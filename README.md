Ghassen Zakraoui
CI Workflow + CD (Deployment) Workflow (ECR + ECS + Fargate) + 3 Levels of Tests
Follow these steps:
1. Clone the repository.
2. `npm install`
3. `npm run build`
4. `docker-compose up`
5. `npm run test` for unit and integration tests.
6. `npm run test:e2e` to run the e2e tests.

